# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Model / HF caches (downloaded on first run)
.cache/
*.safetensors
*.wav

# OS / editor
.DS_Store
.idea/
.vscode/

# Logs
*.log

# Secrets / env (never commit real keys — use .env.example as the template)
.env
.env.local
.env.*.local

# Deploy / Node tooling (Vercel & Netlify CLIs)
.vercel/
.netlify/
node_modules/
